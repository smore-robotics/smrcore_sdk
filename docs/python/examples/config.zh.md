# 配置示例（Python）

## 运行前

这些示例会读取或临时修改机器人配置。它们会在退出前恢复原值（或拒绝覆盖你的
数据），但你仍应先查看打印输出再继续。

## config_limits

### 作用

读取当前关节速度限制，做小幅下调，读回校验，然后恢复原值。

### 适用场景

- 查看和调整运行时运动限制。

### 完整源码

```python
--8<-- "examples_py/config/config_limits.py"
```

## waypoints

### 作用

用当前关节位姿创建一个命名空间化的演示点位，列出全部点位，然后只删除自己创建的
那一条。若演示名已存在则拒绝运行。

### 适用场景

- 管理命名关节点位，不触发运动。

### 完整源码

```python
--8<-- "examples_py/config/waypoints.py"
```

## payload

### 作用

保存当前负载，应用一个小的演示负载，然后在退出前恢复原值。负载字典为
`{"mass": float, "com": [x, y, z]}`。

### 适用场景

- 为动力学相关功能设置负载参数。

### 完整源码

```python
--8<-- "examples_py/config/payload.py"
```
